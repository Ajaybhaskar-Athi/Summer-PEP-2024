#include<bits/stdc++.h>
using namespace std;


/*int main(){
     int p,q,r;cin>>p>>q>>r;
    vector<int>vp(n),vr(n);
    for(int i=0;i<n;i++){
        vp[i]=v[i]*p;
        vr[i]=v[i]*r;
    }

    vector<int>pre_max_vp(n);
    vector<int>suf_max_vr(n);
    pre_max_vp[0]=vp[0];
    for(int i=1;i<n;i++){
        pre_max_vp[i]=max(pre_max_vp[i-1],v[i]);
    }
    suf_max_vr[n-1]=vr[n-1];

    for(int i=n-2;i>=0;i--){
        suf_max_vr[i]=max(suf_max_vr[i+1],v[i]);

}
*/
